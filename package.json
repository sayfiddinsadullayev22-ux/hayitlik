{
  "name": "react-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx server.ts",
    "build": "vite build && mkdir -p dist && cp index.html dist/ && cp -r public/omages dist/omages",
    "start": "tsx server.ts",
    "preview": "vite preview",
    "lint": "echo 'No linting needed for HTML-only build'"
  },
  "dependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/express": "^5.0.6",
    "better-sqlite3": "^12.8.0",
    "express": "^5.2.1",
    "telegraf": "^4.16.3",
    "tsx": "^4.21.0",
    "vite": "^6.2.0"
  },
  "devDependencies": {
    "tailwindcss": "^4.1.14"
  }
}
